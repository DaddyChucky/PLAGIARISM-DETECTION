def verifie_distance(v1, v2, d):
    # En prenant en compte 2 voitures
    # allant respectivement à une vitesse de v1 unité et v2 unité
    # et se trouvant à une distance d l'une de l'autre.
    # imprimé leur point de rencontre.

    # En un langage plus mathématique.
    # Connaissant v1, v2 et d, trouvé x tel que :
    # v1 * t = x = d - v2 *t
    pass


if __name__ == "__main__":
    verifie_distance(2, 1, 12)
    verifie_distance(2, 1, 10)
    # 6.66666667
