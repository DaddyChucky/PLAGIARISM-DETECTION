import math


def verifie_distance(angle, vitesse):
    # En assumant une gravité de 9.8 m/s², et en prenant un angle en degré et une vitesse en paramètre,
    # en négligeant toute autre force que celle gravitationnelle,
    # trouver le point d'impact d'un projectile lorsqu'il touchera la sol à la même hauteur qu'il l'a quitté

    pass


if __name__ == "__main__":
    verifie_distance(30, 10)
    # 8.83699391616774
    verifie_distance(1, 10)
    # 0.35611731329082624
    verifie_distance(18, 40)
    # 95.9649391497915
